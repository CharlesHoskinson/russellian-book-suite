# External adversarial-audit prompt — homoiconic-KG C0+P2 (for GPT-5.5 Deep Research Pro)

Paste the XML below into GPT-5.5 (Deep Research Pro). Attach the branch diff
`git diff 796f131..HEAD` (branch `feat/homoiconic-kg-cutover`) and, if the tool
allows, the files named in `<artifacts>`. This is the Phase-Z external leg that
complements the internal audit at `2026-06-17-homoiconic-kg-c0-p2-internal-audit.md`.

```xml
<adversarial_audit model="GPT-5.5 Deep Research Pro">

<role>
You are a skeptical staff engineer reviewing a partially-completed migration in a
Python monorepo. Your job is to find correctness, parity, determinism, and
methodology defects that the team's own internal audit may have missed, and to
stress-test the plan for the remaining phases. Assume the authors are competent
and the obvious bugs are gone — hunt the subtle ones. Be concrete; cite file:line.
</role>

<context>
A non-fiction "book suite" is migrating its knowledge graph from a triple-stack
(rdflib/SPARQL/SHACL + pyDatalog + an embedded Cozo verifier) to one homoiconic
"EDN front, Cozo back" store: schema/queries/constraints are authored as EDN and
compiled to CozoScript; Cozo (via pycozo) is the single engine behind one seam
(cozo_store). Migration is a test-guarded big-bang: characterization goldens freeze
current behaviour, each port must reproduce its golden, the legacy stack is deleted
only after a cutover gate passes.

Two phases just landed on branch feat/homoiconic-kg-cutover:
- C0 (characterization): frozen goldens for the 8 SPARQL queries (pre-existing),
  the SHACL conformance/violation report (bermuda conforms; a synthetic
  "violating workspace" yields 4 violations), and the D9-D11 thesis-consistency
  defects (bermuda clean; a synthetic violating thesis yields D9+D10+D11).
- P2 (SHACL -> EDN constraints): a single EDN status-vocabulary source feeding the
  claim validator; a pure `compile_constraint(edn, schema) -> CozoScript` lowering
  `defconstraint` to a violation rule `[focus_node, path, message]`; 7 constraint
  EDN files; and a `KG_BACKEND=cozo` path in `validate_shacl` (default stays
  `rdflib`) that runs the compiled constraints over a Cozo store and assembles the
  same `ShaclReport`, proven result-set equal to the C0 goldens.
</context>

<key_design_decisions_to_attack>
1. The JSON-schema claim-record contract is STRICTER than the SHACL/EDN
   constraints, and projectors only emit valid-ledger data — so validate_shacl
   conforms on ALL real workspaces; the constraint gate only ever fires on
   synthetic injected fixtures. The team says this is a pre-existing property
   faithfully preserved. Is that acceptable, or does it mean the entire SHACL
   parity story is untrustworthy / the gate is dead weight that should be removed
   rather than ported?
2. Cross-engine violation reconciliation: the rdflib/pyshacl path is NORMALIZED to
   a canonical form (bare-id focus_node + EDN-authored messages, replacing
   pyshacl's auto-messages) so it can be compared to the Cozo path. Messages are
   remapped keyed on the SHACL property path. Find cases where this mislabels or
   collapses distinct violations, or where focus_node stripping is wrong. Is
   comparing engines on (focus_node, path, message) sound, or should message be
   excluded from the parity contract?
3. The violating "fixture" injects violations as raw RDF triples (rdflib path) and
   as directly-loaded Cozo rows (cozo path) — NOT through the ledger (which can't
   hold them). Is testing the two engines on separately-constructed bad data a
   valid parity proof, or a confound? The canonical golden was regenerated by P2.3
   (it no longer freezes raw pyshacl output); a separate frozen raw-pyshacl golden
   was added with a test asserting normalize(raw)==canonical. Is that sufficient to
   de-circularize the parity claim?
4. A `chapter-section` entity was added to the schema with NO production projector
   (loaded empty), purely so the 6th constraint compiles/executes on synthetic
   rows. Is adding schema + a "live" constraint with no data source a smell that
   misleads, even though it's disclosed?
5. Determinism: goldens are byte-stable via canonical sort + LF pinning; Cozo and
   pyDatalog row order is neutralized by sorting. Find any residual nondeterminism
   (set/dict iteration in compiled output, edn_format set ordering, hash-seed
   dependence, locale/timestamp).
6. The status single-source: VALID_TRANSITIONS is derived from one EDN file; the
   JSON-schema enum is drift-guarded as a set. Find a way the two could still
   diverge, or a transition that changed vs the original literal.
</key_design_decisions_to_attack>

<artifacts>
git diff 796f131..HEAD ; and these files on the branch:
- skills/book-knowledge/scripts/{validate_shacl.py, booklogic_kg.py, claim_validator.py, project_ledger_cozo.py, cozo_store.py}
- skills/book-knowledge/assets/{kg-schema.edn, status-enum.edn, shapes.ttl, claim-record.schema.json}
- skills/book-knowledge/assets/kg-constraints/*.edn
- skills/book-knowledge/tests/{test_constraint_ports.py, test_booklogic_constraint_compile.py, test_validate_shacl.py, test_status_enum_single_source.py, fixtures/violating_workspace.py}
- skills/book-knowledge/tests/golden/kg/{shacl_report_violating.json, shacl_report_violating_raw.json, shacl_report_bermuda.json}
- skills/book-thesis/{scripts/capture_consistency.py, tests/fixtures/violating_thesis.py, tests/golden/consistency/*.json}
- docs/audits/2026-06-17-{kg-shacl-representation-divergence.md, homoiconic-kg-c0-p2-internal-audit.md}
- docs/superpowers/plans/2026-06-17-homoiconic-kg-cutover.md (the remaining-phase plan: P3 retire pyDatalog, P5 cutover/delete)
</artifacts>

<remaining_plan_to_stress_test>
P3 retires pyDatalog: a thesis->Cozo projector + D9-D11 ported to EDN->Cozo
(recursive D10). P5 ports the last RDF readers, reconciles 3 documented RDF<->Cozo
query divergences, flips KG_BACKEND default to cozo + adds a cutover gate, then
deletes rdflib/pyshacl/pyDatalog. KNOWN BLOCKER: book-thesis must import
book-knowledge's Cozo modules, but the cross-skill loader (sibling_skills) resolves
to a STALE installed copy under ~/.claude/skills, distinct from the repo; both
skills have a `scripts` package name collision; book-thesis's venv lacks
pycozo/edn_format. Evaluate the three candidate fixes — (a) sync ~/.claude<-repo and
use sibling_skills, (b) a repo-relative aliased loader, (c) refactor the shared Cozo
code into a common importable location — for correctness, repo self-containedness,
and blast radius. Recommend one.
</remaining_plan_to_stress_test>

<deliverable>
A findings report: each issue tagged CRITICAL/IMPORTANT/MINOR with file:line, a
concrete reproduction or argument, whether it affects REAL data or only synthetic
fixtures, and a fix calibrated so it does NOT break the real `examples/bermuda-manual`
workspace. Then: a verdict on whether C0+P2 is a sound foundation for P3, a
recommendation on the cross-skill import decision, and any disagreement with the
internal audit's CRITICAL (C-1, confidence lower bound — now reportedly fixed) and
its conclusion that the SHACL gate's real-data no-op is acceptable.
</deliverable>

<constraints>
- Distinguish real-data impact from synthetic-fixture-only impact for every finding.
- Do not propose tightening that would break valid bermuda data.
- Prefer fixes expressible as EDN/Datalog or small seam changes over engine swaps.
- Be honest about what is solid; a credible audit is not uniformly negative.
</constraints>

</adversarial_audit>
```
