# External adversarial-audit prompt — P2.4 + P3 (for GPT-5.5 Deep Research Pro)

Paste the XML below into GPT-5.5 (Deep Research Pro). Attach the branch diff
`git diff ae0e096..HEAD` (branch `feat/homoiconic-kg-cutover`; `ae0e096` is the
last externally-audited commit — the end of the prior C0+P2 audit) and, if the
tool allows, the files named in `<artifacts>`. This complements the earlier
`2026-06-17-homoiconic-kg-c0-p2-external-audit-prompt.md` (C0+P2) and covers the
four sprints that landed since: the P2.4 audit-remediation and the whole of
Phase P3 (P3.0 cross-skill bridge, P3.1 thesis projector, P3.2 consistency pass).

```xml
<adversarial_audit model="GPT-5.5 Deep Research Pro">

<role>
You are a skeptical staff engineer reviewing four sequential changes in a Python
monorepo of self-contained Claude Code "skills" (each skill has its own venv and a
top-level `scripts` package). The work continues a test-guarded big-bang migration
of a knowledge graph from a triple-stack (rdflib/SPARQL/SHACL + pyDatalog + an
embedded Cozo verifier) to one homoiconic "EDN front, Cozo back" store. Your job is
to find correctness, parity, determinism, and methodology defects the team's own
internal reviews may have missed, and to judge whether this is a sound base for the
final cutover phase (P5). Assume the authors are competent and the obvious bugs are
gone — hunt the subtle ones. Be concrete; cite file:line. A credible audit is not
uniformly negative: say what is solid.
</role>

<context>
Two skills are involved: `book-knowledge` (owns the unified `assets/kg-schema.edn`,
the `cozo_store` seam, the `booklogic_kg` EDN->CozoScript compiler, the SHACL
validator) and `book-thesis` (the thesis/consistency layer). The migration keeps
the legacy stack running in PARALLEL behind characterization goldens; the legacy
code is deleted only after a cutover gate (phase P5, NOT in this diff).

Four sprints landed, each TDD + two internal reviews (a correctness review and a
quality/simplify pass) before commit:

- P2.4 (commit 5aad995): close an external-audit gap — the EDN->Cozo SHACL port had
  ported tbf:status's sh:in and tbf:confidence's range, plus the text/source-span
  minCount arms, but NOT tbf:status / tbf:confidence sh:minCount. So under
  KG_BACKEND=cozo a claim missing its status or confidence CONFORMED while pyshacl
  flagged it. Added status-present.edn + confidence-present.edn (minCount-via-
  negation). Decided the sh:datatype checks are SUBSUMED by Cozo column typing
  (confidence is a typed Float? column, so a wrong type can't load — a load-time
  error, not a silent conform) and added no datatype constraint. Re-keyed the
  rdflib message remap from bare sh:path to (sh:path, sh:sourceConstraintComponent),
  declaring a :component URI per non-sparql constraint, because status-present
  collides with status-enum on the status path and confidence-present with
  confidence-range on the confidence path (internal audit "I-2", which this task
  activates). Added an internal optional `component` field to the Violation
  dataclass (default "").

- P3.0 (commit b047cb2): cross-skill import bridge. book-thesis must import
  book-knowledge's Cozo modules. New book-thesis/scripts/sibling_skills.py:
  book_knowledge_root() resolves the in-repo sibling FIRST (relative to
  book-thesis's own location), with a ~/.claude fallback only if no sibling exists —
  deliberately NOT installed-first (as book-compose's loader is), because the
  installed copy is stale and lacks the P2 work. Modules load under a synthetic
  alias package `_book_knowledge_scripts`. Fixed booklogic_kg's one absolute
  `from scripts.cozo_store import to_snake` -> `from .cozo_store import to_snake` so
  it loads under the alias. Scoped to cozo_store + booklogic_kg only (NOT the
  claim-side project_ledger_cozo, whose jsonschema/pdfplumber chain would bloat the
  venv).

- P3.1 (commit 2c02e38): thesis->Cozo projector. project_thesis(workspace, store,
  book_id=None) reads thesis/<id>.yaml and loads the root thesis node (keyed
  "thesis" so a sub-argument's normalized parent joins it), sub-arguments, and
  invariants into the shared store. Invariant rows reuse compile_thesis's
  _parse_invariant_formal for a machine-readable subject + pinned-value (`!= lit`)
  / forbidden-value (`== lit`). New :invariant entity in the shared schema. The
  YAML is read-only; ids are authored slugs (deterministic). required_evidence /
  advanced_by_chapters are deliberately NOT projected here (P3.2 needs them and
  loads them itself).

- P3.2 (commit 6a315dd): port the D9-D11 consistency pass (rules/consistency.dl +
  pyDatalog, in scripts/datalog_consistency.py) to EDN/Cozo. Chosen approach: author
  the recursive logic as a multi-rule CozoScript program (rules/consistency.cozo) —
  the booklogic single-rule compiler can't express recursion — run via the raw
  store.query seam; assemble the report by REUSING datalog_consistency's
  DefectReport / _emit_pairs / _value_str / ledger reader / claim-facts loader and
  copying its per-rule detail strings verbatim, so only the FACT SOURCE differs.
  project_consistency_facts mirrors _assert_claim_facts field-for-field (subject
  precedence proj > rec.subject > rec.semantic_class; the have_subjects gate;
  verified-only + dedup-by-id). Added 5 thesis-consistency fact tables to the SHARED
  schema (claim-fact, claim-implies, paragraph-supports, sub-arg-chapter,
  sub-arg-evidence). Parity is asserted vs two frozen C0.3 goldens AND vs the live
  pyDatalog pass on the same workspace.
</context>

<key_design_decisions_to_attack>
1. P2.4 — the (sh:path, sh:sourceConstraintComponent) re-key. Is the pair truly
   unique across all 7 active non-sparql constraints? Are the component URIs the
   exact ones pyshacl emits across pyshacl versions? Adding `component` to the
   Violation dataclass: does any caller, golden, or equality/sort path silently
   depend on it (e.g. dedup that now treats two violations as distinct)? The
   normalized output resets component to "" — can a re-normalize or a cozo-path
   Violation (component="") collide in the (path, component) map with a real entry?

2. P2.4 — the "datatype is subsumed by Cozo Float? typing" claim. Is it actually
   true that a wrong-typed confidence cannot enter the Cozo store (load-time error,
   not silent conform)? What about a confidence that is an int vs float, a bool, a
   numeric string, or null? Does the JSON-schema record contract really forbid every
   wrong type on the real ledger, or is there a projector path that could land a
   non-decimal confidence? Is the rdflib path's pyshacl datatype check still active
   and unmatched by the cozo path for any representable value — a residual parity
   gap the team waved away?

3. P3.0 — repo-sibling-first resolution. Trace book_knowledge_root() in: (a) repo
   dev, (b) a fully-installed ~/.claude checkout, (c) a repo whose book-thesis is
   symlinked/junctioned. Could it ever bind a book-knowledge that mismatches the
   book-thesis running it? The `_book_knowledge_scripts` alias is process-global and
   shared with book-compose's loader (which resolves installed-first): construct a
   scenario (same interpreter) where one loader's cached alias serves the other the
   WRONG copy. Is "separate venvs => separate processes" actually guaranteed for all
   call sites (tests, CLIs, a future in-process orchestration)?

4. P3.0/P3.1 — the absolute->relative import change in booklogic_kg, and reusing
   book-thesis's compile_thesis._parse_invariant_formal from the projector. Any
   import-cycle, partial-init, or context where the relative import fails (e.g.
   booklogic_kg imported as a top-level module, not a package submodule)? Is
   depending on a single-underscore "private" function across a module boundary a
   latent break if compile_thesis changes?

5. P3.1 — fidelity to compile_thesis's RDF model. The projector stores the root
   thesis statement as "" when absent (not null) while polarity/scope go null —
   diverges from the other nullable columns; does any P3.2 query distinguish ""
   from null and thereby diverge? The `!= lit` -> pinned-value / `== lit` ->
   forbidden-value split: is it right for BOTH the parser's op semantics AND every
   formal-clause shape the parser accepts? A sub-argument whose parent is another
   sub-argument (not "thesis") — does the join still hold?

6. P3.2 — rule-for-rule fidelity of consistency.cozo vs consistency.dl. Scrutinise
   the recursion (reaches, transitive_contradiction) and the stratified negation
   (orphan, unreachable_supports, sub_arg_no_chapter, missing_evidence) for any
   input where Cozo's fixpoint/stratification semantics diverge from pyDatalog's:
   cycles in supports (mutually-supporting sub-arguments), a paragraph that supports
   itself, an implies cycle (A implies B, B implies A) feeding transitive_
   contradiction, duplicate/again-asserted facts, null/empty-string subjects or
   values. Does Cozo's `v1 != v2` over String columns match pyDatalog's `!=` for
   values that stringify identically but differ in type (e.g. int 9 vs "9", float
   1.0 vs "1.0", bool)? `_value_str` is reused — but are there values where the
   ledger's inlined form and the projection's form stringify differently?

7. P3.2 — parity methodology. The bermuda golden is all-zeros; is it a MEANINGFUL
   parity case or vacuous (does bermuda actually exercise have_subjects /
   missing_evidence / any firing rule, or is it structurally empty so any
   always-return-nothing port would pass)? The non-tautological weight rests on
   test_violating_matches_live_pydatalog and the end-to-end missing_evidence test —
   are those sufficient to cover declared_conflict, sub_arg_no_chapter, AND the
   transitive/direct skip-set interplay end-to-end (vs only at the rule level)?
   Could the reused _emit_pairs first-per-key selection diverge between the engines
   because Cozo's row order differs from pyDatalog's before the sort?

8. P3.2 — putting 5 thesis-consistency fact tables in the SHARED kg-schema. Does
   this leak book-thesis concerns into book-knowledge in a way that can break
   book-knowledge's own store (REQ-KG-011 "exactly the declared relations"), the
   bermuda SHACL cozo path, or book-compose? Running the full rule prefix + one
   `?[...]` entry per head 8x per call — any rule that errors on an empty relation,
   any nondeterminism, any performance cliff on the real ledger?

9. Determinism across all four: synthetic ids built by `\x1f`-join, dict/set
   iteration, yaml load order, edn_format ordering, hash-seed dependence,
   locale/timestamp. Are the new goldens byte-stable on Windows (CRLF) and POSIX?
</key_design_decisions_to_attack>

<artifacts>
git diff ae0e096..HEAD ; and these files on the branch:
- skills/book-knowledge/scripts/{validate_shacl.py, booklogic_kg.py, cozo_store.py, project_ledger_cozo.py}
- skills/book-knowledge/assets/kg-schema.edn
- skills/book-knowledge/assets/kg-constraints/*.edn  (esp. status-present, confidence-present, status-enum, confidence-range, confidence-range-low, text-cardinality, source-span-present)
- skills/book-knowledge/assets/shapes.ttl  (the SHACL the EDN ports)
- skills/book-knowledge/tests/{test_presence_ports.py, test_constraint_ports.py, test_booklogic_constraint_compile.py, test_kg_schema.py}
- skills/book-knowledge/tests/golden/kg/shacl_report_violating*.json
- skills/book-knowledge/tests/golden/kg-constraints/{status-present,confidence-present}.cozoscript
- skills/book-thesis/scripts/{sibling_skills.py, project_thesis_cozo.py, consistency_cozo.py, datalog_consistency.py, compile_thesis.py}
- skills/book-thesis/rules/{consistency.cozo, consistency.dl}
- skills/book-thesis/tests/{test_cross_skill_cozo_import.py, test_thesis_projector.py, test_consistency_cozo_parity.py, test_consistency_cozo_rules.py}
- skills/book-thesis/tests/fixtures/violating_thesis.py
- skills/book-thesis/tests/golden/consistency/d9_d11_{bermuda,violating}.json
- skills/book-thesis/pyproject.toml
- docs/superpowers/plans/2026-06-17-homoiconic-kg-cutover.md  (tasks P2.4, P3.0, P3.1, P3.2, P5)
</artifacts>

<remaining_plan_to_stress_test>
Phase P5 (cutover, NOT in this diff): P5.1 port the last RDF readers
(query_chapter_evidence, audit_taxonomy) — touches book-compose; P5.2 reconcile 3
documented RDF<->Cozo query divergences; P5.3 flip KG_BACKEND default to cozo + add
a cutover gate — gated on P2.4 (done) AND a full cozo-green suite (KNOWN BLOCKER:
other consumers like run_competency_queries are not yet cozo-green, and the
RDF-injection fixtures are rdflib-pinned); P5.4 delete rdflib/pyshacl/pyDatalog +
the RDF/SHACL/TTL stack + the parallel pyDatalog consistency pass + compile_thesis's
TTL emit. Evaluate: is the P3 work (esp. the CozoScript consistency program and the
shared-schema fact tables) a sound base for flipping the default and deleting the
legacy pass? What must the P5.3 cutover gate assert that it currently might not
(e.g. consistency-pass parity, not just SHACL/query parity)? Is there any consumer
that still reads the TTL/RDF the projector path does not yet feed from Cozo?
</remaining_plan_to_stress_test>

<deliverable>
A findings report: each issue tagged CRITICAL/IMPORTANT/MINOR with file:line, a
concrete reproduction or argument, and whether it affects REAL data (the
examples/bermuda-manual workspace and the real claim/thesis ledgers) or only
synthetic fixtures. Then: a verdict on whether P2.4 + P3 are a sound foundation for
the P5 cutover; a specific list of what the P5.3 cutover gate must assert; and any
disagreement with the team's key claims — that the SHACL datatype checks are fully
subsumed by Cozo typing, that the CozoScript consistency pass is a faithful
non-tautological equivalent of the pyDatalog pass, and that repo-sibling-first
cross-skill resolution is safe.
</deliverable>

<constraints>
- Distinguish real-data impact from synthetic-fixture-only impact for every finding.
- Do not propose changes that would break the valid examples/bermuda-manual workspace.
- Prefer fixes expressible as EDN / CozoScript / small seam changes over engine swaps.
- The legacy stack is intentionally still present (deleted only in P5) — do not file
  "dead code" findings against rdflib/pyshacl/pyDatalog or the parallel passes.
- Be honest about what is solid; a credible audit is not uniformly negative.
</constraints>

</adversarial_audit>
```
