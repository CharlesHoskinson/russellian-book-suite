You are working in the russellian-book-suite repo at `C:\Users\charl\code\russellian-book-suite`. Pull the latest main before starting.

Your mission: produce full operator-facing documentation for the `neurosym-forge` skill. The complete brief is at `docs/handoffs/2026-05-14-neurosym-forge-documentation-brief.md`. Read it end to end — it is self-contained and tells you the mission, the source materials to read first, the four deliverables with target paths and required sections, the style authority (russellian-style + humanizer), the workflow (brainstorm → plan → subagent execution), and the acceptance criteria.

Use the superpowers stack: `superpowers:brainstorming` to confirm scope, `superpowers:writing-plans` to plan, `superpowers:subagent-driven-development` to execute. Use git worktrees for parallel edits. Branch off `origin/main` to a branch named `docs/neurosym-forge-documentation`. Open a single PR titled `docs: full neurosym-forge documentation` when finished.

Style discipline: every new prose file must pass the `russellian-style` linters with zero critical findings and pass a `humanizer` pass. Commits must be terse and human; no AI attribution; no Co-Authored-By.

Begin by reading the brief.
