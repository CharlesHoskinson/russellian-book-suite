?[focus_node, path_node, message] := *claim{id: focus, status: status}, !is_null(status), status != "proposed", !is_null(status), status != "verified", !is_null(status), status != "disputed", !is_null(status), status != "superseded", !is_null(status), status != "refuted", focus_node = focus, path_node = "https://example.org/book-knowledge#status", message = "Claim status must be one of: proposed, verified, disputed, superseded, refuted."