# Chapter 1: CSS Reset

A minimal manuscript paired with an HTML file under test. The lint of interest here is the D7 CSS check; this markdown exists only because the linter requires `manuscript.md` on disk.
