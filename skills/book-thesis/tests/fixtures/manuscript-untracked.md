# Chapter 1: Untracked

A plain assembled paragraph with no supports back-pointer and enough words
to register as prose, exactly as _assemble_manuscript emits chapter bodies.

A second plain paragraph, also carrying no back-pointer, like a real
assembled manuscript that was never annotated with supports carriers.
