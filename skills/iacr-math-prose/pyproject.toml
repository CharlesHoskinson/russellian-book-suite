[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "iacr-math-prose"
version = "0.1.0"
description = "IACR-tier notation, environment, and proof-style conventions for cryptography paper prose"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<10.0",
]

[tool.setuptools]
packages = []
