# Passive Sample

The server is provisioned by the script. Configurations were loaded by the daemon. The request was handled before the timeout. Logs are written to disk every minute.

## Active section

The script provisions the server. The daemon loads configurations.
