[pytest]
# Deselect live (network) tests by default so a future @pytest.mark.live
# test cannot silently start hitting real endpoints in unit runs. Matches
# skills/scrapling-fetch/pytest.ini. Nightly opts back in explicitly.
addopts = -m "not live"
markers =
    live: tests that hit real external endpoints (nightly only)
