{
  "name": "bermuda-verifier-napi-shim",
  "private": true,
  "version": "0.1.0",
  "description": "napi-rs build shim — required because @napi-rs/cli v3 looks for package.json in cwd. The real package.json is at verifiers/bermuda/package.json; this file exists only so `cd rust-verifier && napi build` resolves napi config.",
  "napi": {
    "binaryName": "bermuda-verifier",
    "targets": [
      "x86_64-unknown-linux-gnu",
      "x86_64-pc-windows-msvc",
      "aarch64-apple-darwin",
      "x86_64-apple-darwin"
    ]
  }
}
